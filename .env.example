# =============================================================================
# INTENT CLASSIFIER API - ENVIRONMENT VARIABLES
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env no Git!
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
APP_NAME="Intent Classifier API"
APP_VERSION="1.0.0"
ENVIRONMENT=development  # development, staging, production
DEBUG=true
HOST=0.0.0.0
PORT=8000
RELOAD=true  # Auto-reload em desenvolvimento (somente dev)

# -----------------------------------------------------------------------------
# Google Gemini Configuration
# -----------------------------------------------------------------------------
# OBRIGATÓRIO: Obtenha sua chave em https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Modelo Gemini a ser usado
GEMINI_MODEL=gemini-2.5-flash  # Opções: gemini-2.5-flash, gemini-2.0-flash, gemini-1.5-flash, gemini-1.5-pro

# Temperatura da geração (0.0 = determinístico, 2.0 = criativo)
GEMINI_TEMPERATURE=0.3

# Máximo de tokens na resposta
GEMINI_MAX_TOKENS=512

# Timeout para chamadas ao Gemini (segundos)
GEMINI_TIMEOUT=30

# -----------------------------------------------------------------------------
# Few-Shot Configuration
# -----------------------------------------------------------------------------
# Caminho para o arquivo de exemplos few-shot
EXAMPLES_FILE_PATH=src/data/examples.json

# Número máximo de exemplos no prompt
MAX_EXAMPLES_IN_PROMPT=5

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Formato de log: json ou text
LOG_FORMAT=json

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Origens permitidas (separadas por vírgula, ou use "*" para todas)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,*

# Permitir credenciais (cookies, authorization headers)
CORS_ALLOW_CREDENTIALS=true

# Métodos HTTP permitidos
CORS_ALLOW_METHODS=*

# Headers permitidos
CORS_ALLOW_HEADERS=*

# -----------------------------------------------------------------------------
# Rate Limiting
# -----------------------------------------------------------------------------
# Habilitar rate limiting
RATE_LIMIT_ENABLED=true

# Número de requisições por minuto
RATE_LIMIT_REQUESTS=100

# -----------------------------------------------------------------------------
# Security (Produção)
# -----------------------------------------------------------------------------
# Em produção, configure estas variáveis adicionais:
# SECRET_KEY=your_secret_key_here_generate_with_openssl_rand_hex_32
# ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com
